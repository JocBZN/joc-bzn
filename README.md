# JOC-BZN-Mobile 🎮

A mobile **survivors-like** (bullet-heaven) game — think *Vampire Survivors* / *Brotato* with a **cyberpunk** theme. Built in **Godot 4.7** with GDScript, targeting **Android**.

> **AI assistant reading this (hi 👋):** the most important section is **"Working with the project owner"** at the bottom. The owner is a **complete beginner** being taught step by step, in Romanian.

## Game concept
- Top-down arena. The **player** appears centered (a `Camera2D` follows them) but moves freely across a large world.
- **Enemies** spawn continuously from off-screen and chase the player.
- The player **auto-fires** projectiles at the nearest enemy. Enemies have HP and die.
- Enemies touching the player deal **contact damage**; the player has HP + a health bar.
- **Roguelike core (planned):** dead enemies drop **XP** → XP bar fills → **level up** → choose **1 of 3 items/upgrades**. Difficulty ramps over time (enemies faster/tougher, drop more XP).

## Tech & conventions
- **Engine:** Godot 4.7 stable · 2D · "Mobile" renderer. Stretch mode `canvas_items`, aspect `expand` (scales to any phone screen).
- **Language:** GDScript.
- **Indentation: TABS.** Godot rejects mixed tabs/spaces — this is *the* most common error when pasting code. After any paste use **Edit → Convert Indent to Tabs**.
- **Groups** are used to find nodes across the tree: the player is in group `"player"`, enemies in group `"enemy"`. Look up with `get_tree().get_first_node_in_group("player")` and `get_nodes_in_group("enemy")`.
- `get_*_in_group` returns a generic `Node`, so **cast with `as Node2D`** before touching `global_position` (otherwise "cannot infer type" errors). Dynamic property access on such nodes yields harmless **yellow** warnings (yellow = warning/OK, red = error/must fix).

## Project structure
All scenes (`.tscn`) and scripts (`.gd`) live in the project root.

- **`main.tscn`** — the game world (root `Node2D` "Main"):
  - `Ground` (Sprite2D + `ground.gd`) — the infinite repeating grass that follows the player.
  - `World` (Node2D, `y_sort_enabled`) — depth-sorted container holding `Props` (trees), `Rocks` (stones), `Player`, and (added at runtime) the enemies, so they overlap correctly by depth.
  - `Props` (`props.gd`) / `Rocks` (`rocks.gd`) / `DesertStructures` (`desert_structures.gd`) — chunk-based procedural, deterministic spawners (infinite). Rectangle hitboxes with per-side tuning (`hitbox_north/south/east/west`), `sort_anchor`, `min_gap_hitboxes` (min spacing). **Biome rules:** trees avoid the desert *and* its soft gradient; rocks avoid only the hard desert (they may sit on the gradient); desert structures (cactus/house/monument) spawn *only* in the desert — see `BiomeMap.is_desert_chunk` / `desertness_at_chunk` / `desert_inset_chunk`.
  - `Spawner` (Node + `spawner.gd`) — a Timer that instances enemies around the player (into `World`).
  - `HUD` (CanvasLayer + `hud.gd`) — screen-fixed UI: health bar + XP bar + level, all built in code.
  - `LevelUp` (CanvasLayer + `levelup.gd`) — the level-up choice screen (3 of **35** upgrades, **weighted by rarity**), styled like *Megabonk*: an ornate `Menu.png` panel with each choice framed by a **rarity border** (Common→Legendary) + matching colored text; pauses the game.
    - ⚠️ **Rarity is a real drop rate, not just a colour** — `RARITY_CHANCE` at the top of `levelup.gd`: Common 30 · Uncommon 30 · Rare 20 · Epic 15 · Legendary 5. The roll picks the **rarity first**, then an item within it, so **how many items a tier holds no longer changes how often that tier appears** — adding a Legendary keeps Legendary at 5% and simply splits it further. (Before this, selection was uniform over the whole list, which made Legendary show up in 11.4% of rows.) Each entry is one dict in `UPGRADES` (id / name / icon / rarity / description) and its real effect is a `match` arm in `_apply()` — **the description text is not the source of truth, `_apply()` is.**
- **`player.tscn`** (`CharacterBody2D` + `player.gd`) — has an **AnimatedSprite2D** (8-directional run + idle poses, `player_frames.tres`), CollisionShape2D, Camera2D. Handles arrow-key movement, auto-fire at nearest enemy (Timer), HP + a contact-damage tick, the fire trail (Firewalker), and death (opens the Game Over screen).
- **`enemy.tscn`** (`CharacterBody2D` + `enemy.gd`) — chases the player, has HP, `take_damage()`, dies via `queue_free()`. Uses an **AnimatedSprite2D** with **8-directional walk animations** (`enemy_frames.tres`, 6 frames each, picked by angle in `enemy.gd`'s `DIRECTII`); the art lives in `homeless directii/frames/`.
- **`bullet.tscn`** (`Area2D` + `bullet.gd`) — flies in a direction, on `body_entered` damages bodies in group `"enemy"`, self-destructs after `lifetime`. Supports **pierce**, **knockback**, **crit**, and **explosive AOE** (Jean's Bomb).
- **`firetrail.gd`** (script-only, instanced from `player.gd`) — a fire patch dropped at the player's feet while moving (**Firewalker** upgrade): plays the fire animation, burns enemies in range on a tick, rotates to the movement direction, renders **under** the actors (`z_index`), and fades after a per-stack duration.
- **`interact_ui.gd`** — one big screen-space **SUMMON** button pinned to the **left edge** (phone-friendly: fixed spot, thumb-sized, doesn't drift with the camera). It appears only when a not-yet-used statue is within range and targets the nearest one; statues themselves just join the `"statue"` group and expose `poate_invoca()` / `invoca()`.
- **`statues.gd`** — spawns statues procedurally, chunk by chunk like trees and rocks: **3% of chunks get exactly one**, at a deterministic position that keeps clear of nearby trees **and rocks** (crowded chunks simply get none, costing ~0.4%). Every run also **starts you at a random point** in the infinite world (`spawner.gd`, saved to `GameSettings.run_spawn`), so the map is different each time without touching the generation math.
- **`statue.tscn`** (`StaticBody2D` + `statue.gd`) — an interactive world statue with a **visually-editable** collision box. Its look is rolled at spawn from three variants (`harta/Statue Version 1..3.png`) with weights **60% / 30% / 10%** — edit the `VARIANTE` list at the top of `statue.gd` to change the odds. When the player is near, a small **"Summon"** button appears. Pressing it plays a one-shot sequence: alert symbol → statue **sinks into the ground** (its collision disables) → **screen shake** (camera-offset earthquake) → a **boss rises slowly from the ground**. All timings/offsets are `@export`.
- **`garda.tscn`** (`CharacterBody2D` + `garda.gd`) — the **"Garda" boss**, spawned *only* by the statue's Summon. Slower and much tankier than a normal enemy, walks toward the player using **8-directional** animations, and **throws lightning balls** from range on a cooldown. Static `garda_0.png` shows while it's rising; the walk animation kicks in once it moves. **(2026-07-19) Special attack:** every **10s** it fires the normal attack **3 times in a fast burst** (`special_interval` / `special_shots` / `special_gap`, ~0.12s apart). The burst is driven by a counter in `_physics_process`, **not `await`** — a coroutine would resume on a freed node if the boss dies mid-burst. It re-reads the player each shot, so the burst tracks you. Its lightning frames now carry a **2px black outline** (added with `scripts/stroke.ps1`; re-run `--headless --import` after touching any PNG or the game keeps using the cached old one).
- **`lightning.tscn`** (`Area2D` + `lightning.gd`) — the boss's ranged projectile: a violet lightning ball with a **circle hitbox**, flies toward the player, and only damages group `"player"` (`take_damage`). Made extra visible via slow frames + `modulate > 1` (glow).
- **Boss art** lives in `boss/` (walk GIFs split into `walk_<dir>_<i>.png` frames + the lightning-burst frames); the alert symbol in `Upgrades/symbol_alert_002_large_red/`. New GIFs are split to PNG with PowerShell + `System.Drawing`; **open the project in Godot once to import new PNGs** before they render (art is loaded at runtime with `load()`).

## Weapons
Picked in the main menu (`GameSettings.weapon_type`, read by `player.gd` on `_ready`). All four share the **same base stats** (`bullet_damage` 10, `fire_interval` 0.5, `bullet_speed` 700) — `weapon_type` only changes *behaviour*, in four places in `player.gd`:

| Weapon | How it fires |
|---|---|
| **Pistol** | One bullet at the nearest enemy. Nothing else. |
| **Mage Staff** | The same bullet, re-skinned as an animated orb, that **explodes on impact** (radius 110, damage = 60% of `bullet_damage`). |
| **Extinguisher** | No bullets at all — an **aura** pulses around you every `fire_interval`, hitting *every* enemy within `aura_base_radius + level × aura_growth`, for `aura_damage + 50% of bullet_damage` (15 at start). |
| **Cursed Sword** | No bullets — a **melee slash** in the direction you're **facing** (`_facing`), hitting *every* enemy inside a fixed rectangle that runs from the player out to the animation's furthest pixel, for `sword_base_damage + bullet_damage`. Built on the **Firewalker model**: `sword_size` is the width in px, and the rectangle is *measured from the art* at startup, so art and hitbox cannot drift apart. Starts **slow** (`fire_interval × sword_slow_start` when picked) and speeds up with attack-speed upgrades. The slash animation is a **child of the player**, so it follows you (feels like the sword is always in hand), and draws **under** him. Scales with damage / crit / knockback / instakill / weapon-size upgrades. |

**Collision:** everything is on the default layer/mask (layer 1). Bullets (Area2D) detect enemies (CharacterBody2D) via `body_entered` and filter with `is_in_group("enemy")`, so no manual collision-layer setup is needed yet.

## Current state (2026-07-21, performance pass)
*The game used to crawl ~2 minutes into Final Swarm — 144 FPS → 4 FPS. Measured, not guessed: an instrumented run that jumps straight to 10:00 and logs FPS + a per-script node census every 5s.*
- 🐞 **XP gems were never removed** — the whole cause of the late-game slowdown. In Final Swarm ~30 enemies die per second and you cannot possibly collect them all, so the gems piled up forever: **5390 gems / 17k nodes at 13:00**, each an `Area2D` animating itself every physics frame. Now capped at **`MAX_GEME = 200`** (`xp.gd`): when a new gem would exceed the cap, the gem **farthest from the player** pours its value into the new one and disappears. **No XP is lost** — the same total sits on the map, concentrated into fewer gems, and the ones that survive are the ones near you. A gem that swallowed others is drawn slightly bigger (up to `SCALE_MAX`). Result: nodes flat at ~1500, **144 FPS for the whole run**.
- 🐞 **Thunder God spawned one arc per enemy in range, per impact.** In a crowd that is quadratic: **4400 live arcs**, each an `AnimatedSprite2D` with a per-frame `_process`, dragging 300 enemies down to **6 FPS**. Two caps in `player.gd`, both **visual only — every enemy in range still takes the damage**: `THUNDER_MAX_ARCE = 10` arcs drawn per discharge, and `THUNDER_MAX_ARCE_VII = 60` arcs alive on screen in total. Past ~10 overlapping arcs you can't read them anyway.
- ✅ **Enemy hit-flashes are a float countdown, not a Tween** (`enemy.gd`). One `Tween` object per hit is clean until an aura + Thunder God hit 300 enemies several times a second — thousands of allocations per second for a 0.12s colour fade. `_flash_time`/`_flash_dur`/`_flash_color` are drained in `_physics_process` and lerp to the current tint; behaviour is identical (and now the tint tracks the freeze colour *live*). Also: enemies **cache the player node** instead of doing a group lookup every frame (×300), and only call `anim.play()` when the direction actually changed — the same rule `player.gd` already followed.
- ✅ **`Fx` has budgets** (`fx.gd`): at most **45** damage numbers, **25** spark bursts and **35** glow flashes alive at once; requests past the cap are skipped. An aura pulse catching 200 enemies asked for 200 `Label`s in one frame. ⚠️ The counters are reset in `_world()` whenever the current scene changes — the tweens that decrement them belong to nodes in the old scene and die with it, so without the reset the budgets would stay stuck full and **all effects would vanish after a few restarts**.
- ✅ **`Audio.play()` rate-limits per sound name** (`MIN_GAP_MS = 45`). The hit/death sounds are currently missing from `SFX` so nothing changes today, but when they are added back, Final Swarm would ask for them hundreds of times a second — a wall of noise and a pointless cost.
- ℹ️ **Measured result** (worst case: 300 enemies, the spawner's cap, plus Thunder God): **6 FPS → 90-144 FPS**. With a fast-killing build, a full run stays pinned at the refresh rate. The 300-enemy cap itself (`spawner.max_enemies`) is **not** the problem — 300 bodies with no Thunder God ran at 118-144 FPS even before this pass.

## Current state (2026-07-19, later session)
*Combat & FX pass. Each item is described in full where its feature lives — this is the index.*
- ✅ **Thunder God arcs stay glued to enemies** (`electric_arc.gd`) instead of hanging where they spawned, and the chain now fires from the **Extinguisher aura** too — one chain per pulse, from a random enemy it hit. See the Thunder God entry below.
- 🐞 **Plugged In was doing nothing at all** since it was added on 2026-07-17 — fixed. See its entry below; this is the one change that alters run balance.
- ✅ **Panic Button is now an earthquake + shockwave** (`shockwave.gd`) that deals its damage as the front sweeps outward, Mama Mega style.
- ✅ **Cacti cast ground shadows**, sharing `ground_shadow.gd` with the trees.
- ✅ **The Garda boss got a 10s burst special** (3 rapid lightning balls) and its lightning frames got a 2px black outline.

## Current state (2026-07-19)
- ✅ **New tree art** — `harta/trees/Tree Variant 1..7.png` replaces the old `spr_tree_1..16.png`. **16 variants → 7**, canvas **64×64 → 128×128**. Trees are then **1.5× larger** than the old ones by request (`tree_scale` 2.775).
- ✅ **Trees cast a ground shadow.** A squashed radial gradient (built once in code, reused by every tree — no asset file) pinned to the visible base of the trunk, at **`z_index = -1`** so it stays on the ground: under the tree, under the player, and under other trees regardless of Y-sorting. Same trick the fire trails use. Tunable per-instance: `shadow_alpha` / `shadow_width` / `shadow_squash` / `shadow_shift_y`. Width is measured from the tree's **visible** contour, not the canvas, so it stays correct if the art changes again. **(2026-07-19) Cacti have one too**, and the whole thing (including the pixel-scanning that finds the trunk) now lives in **`ground_shadow.gd`** so there's a single copy. It has **no `class_name`** on purpose — global names only register when the project is opened in the editor, which breaks running the game from the command line; use `const GroundShadow := preload("res://ground_shadow.gd")`. Desert structures opt in via an optional `shadow` key in `CONFIG` (only the cactus has one; houses/monuments would look wrong with a squashed ellipse under a straight wall). The cactus uses `width` **0.85** vs the tree's 0.60 — the value is a fraction of the object's contour, and a cactus is far narrower, so the same fraction gave a barely-visible smudge.
- ✅ **Tree collision boxes and shadows are measured from the trunk**, via `_trunk()` — it scans the bottom 18% of the drawing and takes the span of opaque pixels (trunk + roots). The box sits with its **bottom edge on the roots**, centred on the trunk. Before this, `hitbox_factor` was a fraction of the **canvas** width and the Y came from `sort_anchor`, which produced a wide flat bar floating in the middle of the canopy — you got stuck in leaves and walked through trunks. Shadows had the same bug: centred on the whole contour, so on lopsided trees they landed beside the tree.
- ⚠️ **If you swap tree art again, `tree_scale` is now the only value that needs recomputing** — hitboxes and shadows fit themselves. And don't just divide by the canvas ratio: what matters is how much of the canvas the drawing fills (old art ~40×49 of 64; new art ~97×120 of 128 → ratio 1.85, not 2.25). `sort_anchor` stayed 0.35 — measured, the sort line lands at 31% of the old tree's height vs 34% of the new. The per-side `hitbox_north/south/east/west` overrides were **cleared from `main.tscn`** (they were calibrated for the old box and would distort the new one), then Răzvan re-tuned them by hand on the new geometry — see below.
- ℹ️ **Final hand-tuned tree collision values** (Răzvan, in the editor, on top of the trunk-derived box): `hitbox_east` and `hitbox_west` **0.5 each**, so the box ends up **2× the trunk-derived width** — the bare trunk felt too thin to walk into. `hitbox_north` **0.2** with `hitbox_south` **−0.2** (the latter on the `Props` node in `main.tscn`) shifts the box up without changing its height. `sort_anchor` nudged 0.35 → **0.355**. Treat these as taste, not math — the derived box is the baseline they modify.
- ✅ **Animated logo instead of the text title.** The old 78px "Nicotine & Knives" label is gone; the menu now shows the wooden logo from `menu/Title/` (4 frames, 256×256) cycling **ping-pong at 0.4 s/frame** — slow, so the cigarette smoke drifts. Knobs at the top of `menu.gd`: `TITLE_FRAME_TIME`, `TITLE_SIZE`. Falls back to the old text label if the frames are missing.
- ✅ **Menu buttons are wood-coloured, set from two constants** at the top of `menu.gd`: `BTN_MAIN` (`#9e603f`, the fill) and `BTN_SECOND` (`#594232`, the border). `hover`\`pressed` are derived with `.lightened()`, so changing those two constants restyles every big button (including the BACK ones) — don't hardcode new shades. They are **opaque on purpose**: with alpha, buttons over the bright sky read lighter than buttons over the dark grass. The weapon-select and shop *buy* buttons still have their own green\cyan styling.
- ⚠️ **The main menu is height-constrained — 648px is the reference and it is nearly full.** The 5 buttons alone eat 346px (5 × 58 + 4 × 14), so logo + spacing must stay under ~274px or **LEADERBOARD falls off the bottom**. Currently 240 + 16 = 256. If you enlarge `TITLE_SIZE` or add a row, check the bottom of the screen.
- ℹ️ **The "CYBER SURVIVOR" subtitle was removed** when the logo landed — the name is already in the logo and the cyan text clashed with the wood. One `_center_label` in `_build_main()` brings it back (drop `TITLE_SIZE` by ~40 to make room).
- ✅ **Animated main-menu background + intro sequence.** The background **animates from the very first frame**; the menu opens on a **clear** one for 1s (no title, no buttons), then a **gaussian blur** fades in together with the game title *centred on screen*, the title holds there for a beat, and finally **slides up to its place** while the buttons fade in behind it. Timings/strength are constants at the top of `menu.gd` (`INTRO_CLEAR`, `INTRO_FADE`, `INTRO_HOLD`, `INTRO_RISE`, `INTRO_BUTTONS`, `MENU_BLUR`); the blur itself is `menu/menu_blur.gdshader` (5×5 kernel, `blur_amount` in pixels).
- ✅ **Tapping the screen skips the intro** and jumps straight to the finished menu (`_input()` → `_skip_intro()` in `menu.gd`). If you add anything to the intro, it must also be forced to its end state there, and any tween you start must go into `_intro_tweens` — a live tween will keep writing over the skipped state. `_play_intro()` re-checks `_intro_running` after every `await` for the same reason.
- ⚠️ **Don't put the title back inside the button `VBoxContainer`.** It lives in its own fixed-size slot (`_title_group`, a plain `Control`) with the logo (`_title_mover`) anchored inside it, and the buttons stay `visible` the whole intro (alpha 0 + `disabled`) so the layout never reflows. Both details exist because the title used to **jump** when the buttons appeared — a container resizing, not an animation bug. Animating a container child's `position` does nothing; the container rewrites it every layout pass.
- ✅ **Frames are cross-faded, because 10 fps alone looks choppy at 60 fps.** `_bg_next` is a second `TextureRect` stacked on the first holding the *next* frame, its `modulate.a` ramping 0→1 across each source frame. Measured: frame-to-frame change went from "34 of 41 frames identical, then a jump" to evenly spread (max/mean 7.4 → 1.9).
- ⚠️ **`menu_blur.gdshader` must multiply by the incoming `COLOR`.** It writes to `COLOR`, and the incoming value carries `modulate` — overwrite it and every `modulate.a` you set on the background is silently ignored (this is what broke the cross-fade at first). Both branches need it, including the no-blur shortcut. **`MODULATE` does not exist in Godot 4.7**; using it makes the shader fail to compile, Godot falls back to the default shader, and you get *smooth motion with no blur* — which reads as success in measurements. Grep stdout for `SHADER ERROR`.
- ⚠️ **The background is a frame sequence, not a video — on purpose.** Godot only decodes **Ogg Theora** (`.mp4`/H.264 is not in the engine), and every Theora conversion we produced came out **corrupted** (confirmed independently: the source mp4 decodes clean, the `.ogv` fails in ffmpeg's *own* decoder with `unpack_block_qpis`, 92% error rate). So `menu/main menu background.mp4` is sliced into **60 WebP frames** in `menu/bg_frames/` (640×360, 10 fps, 6 s) played **ping-pong** (forward then backward) so the loop has no visible seam. **Do not "upgrade" this back to a VideoStreamPlayer** without re-testing decode.
- ℹ️ **Why the frames are low-res:** they are seen blurred for all but the first second (the blur starts at 1s and never leaves), so 640×360 is invisible in practice while 720p×60 frames would cost **~440 MB of VRAM** — unacceptable on the Mobile renderer. Total on disk: **0.8 MB**, ~53 MB VRAM. The sharp first second now shows those same 640×360 frames upscaled (the price of having the background move from the start); on this flat, painted art it does not read as soft. `menu/bg_still.webp` (720p) is kept **only as a fallback**.
- ℹ️ **The source mp4 is gitignored** (79 MB, over GitHub's comfort limit). It lives only on Răzvan's disk; the generated frames are what's committed. To regenerate them you need `ffmpeg` (installed via `winget install Gyan.FFmpeg`).
- ✅ **The Nightclub is Epic again** (was moved Epic → Rare on 2026-07-16, now moved back). Reverses that day's tweak; **Grinder stays Common**, only The Nightclub went back. Rarity split is now Legendary 3 · Epic 7 · Rare 6 · Uncommon 8 · Common 6.
- ✅ **New item — Megane's Katana** (Rare, `upgrade_33.png`): **crit chance scales with how fast you're moving** — Diesel Power's twin, same speed input, different currency. +15% crit at your starting speed, up to **+30% at the 2× cap**, **0 standing still**, additive per pick and stacking on top of Adrenaline's flat crit (`crit_chance_now()` caps the total at 100%, as Adrenaline always did). Both items now share one `speed_ratio()` helper and one `speed_ratio_cap` knob (the old `diesel_speed_cap` was renamed, since it is no longer Diesel-only).
- ✅ **New item — Panic Button** (Epic, `upgrade_34.png`): **100 flat damage to every enemy on the map, once, the moment you pick it** — then the item is spent and does nothing for the rest of the run. Damage is deliberately flat: it skips `damage_mult()` and cannot crit, because it's a detonation, not a weapon hit. Red damage numbers on every enemy (`Fx` runs with `PROCESS_MODE_ALWAYS`, so the numbers survive the level-up pause). Pool is now **30 upgrades**. **(2026-07-19) It's now an earthquake + a shockwave, like Binding of Isaac's Mama Mega:** `shockwave.gd` expands a ring from the player and damages each enemy **as the front reaches it**, so the ones next to you die first and the blast rolls outward. The quake is a new mechanism — `start_quake(dur, strength)` **re-charges** camera trauma every frame for its duration, because `add_shake` is a single spike that decays in ~0.15s. Wave radius is computed from viewport ÷ camera zoom (**1008px** at current settings), not hardcoded; enemies spawn at 700px and walk inward, so coverage is unchanged in practice — but raise `spawn_distance` past ~950 and Panic Button starts missing people.
- ⚠️ **Panic Button is a knowingly weak Epic.** You are always paused and safe when you pick it, so it can never rescue you from an encirclement — in practice it just hands you the XP from whatever was on screen. Răzvan was shown the alternatives (auto-fire when under 20% HP; fire on every level-up) and chose the literal one-shot on purpose. If it feels like a dead pick in play, the fix is to change *when* it fires, not how much it hits for.
- ℹ️ **The icons look swapped, and that's intentional.** `upgrade_33` (Megane's Katana) is a **dog**; `upgrade_34` (Panic Button) is a **bloody katana**. Confirmed with Răzvan — do not "fix" this.
- ✅ **Percentage damage is now a live multiplier, not a number baked into `bullet_damage`.** New `player.damage_mult()` (modelled on `weapon_size_scale()` — a derived factor read at use time) is applied to the **final damage of a hit, exactly like `crit_mult`**, so it works on *every* weapon including the Extinguisher and the Cursed Sword. It multiplies what `bullet_damage` feeds (bullets, mage explosion, aura, sword) but **not** the fire/frost trails — same rule the existing damage upgrades follow. Three new items ride on it; pool is now **28 upgrades**.
- ✅ **New item — Theo's Wrath** (Uncommon, `upgrade_30.png`): **+15% damage while under 20% max HP**, +10% per extra pick (15 → 25 → 35%), following the Hacksaw "base vs stack" pattern (`_theo_taken`). It **switches itself on and off** as your HP crosses the threshold — which is exactly why it can't be written into `bullet_damage` once. Threshold knob: `theo_hp_threshold`.
- ✅ **New item — Cigarette Pack** (Common, `upgrade_31.png`): **+5% damage**, **additive** per pick (5 → 10 → 15%). It lives in the multiplier rather than compounding into `bullet_damage` because **+5% of an integer rounds badly**: `round(10 × 1.05) = 11`, i.e. +10% — double what the card promises. In the multiplier it adds exactly.
- ✅ **New item — Diesel Power** (Uncommon, `upgrade_32.png`): **damage scales with how fast you're actually moving**. Bonus = `diesel_per_stack` (0.15) × stacks × `clamp(velocity.length() / _speed_base, 0, diesel_speed_cap)`. Standing still gives **nothing**; moving at your starting speed gives +15%; speed bought *during the run* (Weird Concoction, Alex's Protection) pushes it higher, capped at **2× start speed = +30%/stack**. The cap exists because Alex's Protection compounds move speed without limit. `_speed_base` is captured in `_ready` **after** `_apply_meta()`, so the shop's Speed levels don't hand you a free head start on the bonus.
- ✅ **Twin Comets now gives +2 projectiles** (Legendary, `upgrade_19.png`), described as **"+2 Parallel Projectiles"**. `bullet_count` goes 1 → **3** → **5** → 7 per pick. No change was needed in `player.gd`: the fire loop centres any count itself (`offset = (i - (bullet_count - 1) / 2.0) * bullet_spacing`), so an **odd** count always keeps one bullet dead centre — on the old single bullet's line — which the previous even counts never did.

## Current state (2026-07-17)
- ✅ **New item — Duridama** (Legendary, `upgrade_45.png`): **1% chance per hit to gild an enemy** — it freezes in the exact frame it was hit (`anim.pause()`, movement stopped) with a golden `modulate`, taking no damage; the **next hit instakills it and drops 2× XP**. Lives in `enemy.gd` (the gilded state belongs to the enemy); the player only exposes `duridama_chance()` (`duridama_stacks * 0.01`, capped at 1.0). Pool is now **40** upgrades.
  - ⚠️ **The 2× XP is applied after rounding**, not before: at a fractional `xp_mult` (e.g. 2.6), doubling the pre-rounded value gives round(2.6)=3 vs round(5.2)=5 — a 1.7× ratio. Rounding the normal drop first, then doubling, gives exactly 2×.
  - ℹ️ **Luck does not boost Duridama's chance** (unlike crit/instakill) — a deliberate call to avoid making a guaranteed-instakill Legendary even more frequent. Add `+ luck_bonus()` to `duridama_chance()` if you want it to.
- ✅ **Rabbit's Foot now gives +25% Move speed, not attack speed** (`p.speed *= 1.25`, compounding like Alex's Protection). The −5 damage tradeoff is unchanged. (Supersedes the older "+25% attack speed" note below.)
- ✅ **Twin Comets now multiplies the bonus projectiles too**, not just the main salvo. The parallel spread was extracted into `_fire_volley(origin, dir, …)`, called both for the primary target and for each Stacked Armory / Broken Watch bonus target — so every bonus shot is a full volley. Measured: `bullet_count=3` + 2× Stacked Armory fires **9** bullets (3 + 2×3); it fired 5 before.
- ✅ **New item — Tome of Knowledge** (Rare, `upgrade_44.png`): **50% less XP to level up**. Same one-time-multiply pattern as Grinder (`xp_to_next *= 0.5`); because each level's threshold is derived from the current `xp_to_next` (×1.2 per level), the cut carries forward to every future level. Stacks (second pick → 25% of original). Pool is now **39** upgrades.
- ✅ **Two more Luck items — The Office** (Uncommon, `upgrade_40.png`): **+2.5 Luck · +5% Attack Speed** — and **Royal Flush** (Epic, `upgrade_42.png`): **+10 Luck**. All three Luck items stack (2.5 + 5 + 10 = 17.5). Pool is now **38** upgrades.
  - ⚠️ **`player.luck` is a `float`, not an `int`** — The Office grants +2.5, and an integer would silently swallow the half on every pick. The STATS row prints `("%.1f" % luck).trim_suffix(".0")` so 2.5 stays "2.5" while 5.0 shows as "5".
  - ℹ️ **`upgrade_fire_rate(0.95)` = "+5%"** by the project's existing convention (Rolling Papers uses `0.90` for "+10%"). The factor multiplies the *interval*, so it is really +5.3% shots/sec. Kept consistent rather than fixed for one item.
- ✅ **New item — Unusual Clover** (Rare, `upgrade_43.png`): **+5 Luck**, a new stacking stat that does two separate things. It **tilts the rarity odds** (`LUCK_TAKE` / `LUCK_GIVE` in `levelup.gd` — per point it takes 0.5 from Common and Uncommon and splits it 2:2:1 into Rare/Epic/Legendary, so 5 Luck = −2.5/−2.5/+2/+2/+1) and **adds +0.4pp per point to chance-based items** (`LUCK_CHANCE_PER` in `player.gd` — crit, instakill, Broken Watch, Plugged In; 5 Luck turns a 15% crit into 17%). Pool is now **36** upgrades.
  - ℹ️ **Luck never grants a chance you didn't pick.** Without Adrenaline your crit stays 0%, not 2% — `crit_chance_now()` / `instakill_chance_now()` return 0 when the item is absent. Deliberate; change it there if you want Luck to seed those from scratch.
  - ⚠️ **`_sanse_cu_noroc()` clamps at 0 for a reason.** Past ~60 Luck, Common would go negative, and a negative slice on the weighted wheel makes the *next* rarity swallow the difference. Clamped, only what is actually taken gets redistributed, so the total stays 100 at any Luck.
  - ⚠️ **The STATS panel is fixed-height and now runs 13 rows.** Adding "Luck" pushed the last row over the frame; row spacing went 7 → 3 to fit. Check the panel's bottom edge if you add another stat.
- ✅ **New item — Undying Spirit** (Legendary, `upgrade_41.png`) + the **LIMBO** mechanic (`limbo.gd`, node `Limbo` in `main.tscn`): your first death sends you to an empty black-and-white world with **50% HP** and a burst of enemies instead of Game Over; survive **1:00** and you return to the exact spot you died, with the enemies that killed you gone. **Once per run** — a second pick does not grant a second life; dying in Limbo is a real Game Over. Pool is now **35** upgrades.
  - ⚠️ **Limbo does not load another scene** — it strips the current one: the four world generators are disabled, hidden **and emptied**, the normal spawner is stopped, `Difficulty` is frozen, and a grayscale shader (`limbo_bw.gdshader`) covers the screen. If you add a new world generator, it must be added to `WORLD_NODES` or it will still be there in Limbo.
  - ⚠️ **Emptying a generator means clearing its `_loaded` dict too**, not just freeing its children — otherwise it thinks those chunks already exist and the world stays permanently empty after you return.
  - ⚠️ **The grayscale filter sits on `layer = 5`, above the HUD** — anything drawn by the HUD is desaturated too. The Limbo countdown is therefore a `Label` inside `limbo.gd`'s own CanvasLayer, added **after** the overlay so it draws on top and stays red; the HUD hides its own timer meanwhile. Put anything that must stay coloured in Limbo there, not in `hud.gd`.
  - ℹ️ **"Difficulty from a minute ago"** is `Difficulty.mult_time_override`, which feeds **only** the enemy multipliers. The on-screen clock and the Final Swarm announcement still read `time`, so the run clock can be frozen without breaking the HUD.
- ✅ **New item — Plugged In** (Rare, `upgrade_39.png`): **"10% chance to chain lightning on hit"** — a cheaper Thunder God that fires the exact same chain (`thunder_burst`) but only on a roll, **+10% per pick** (first pick = 10%, capped at 100%). The per-hit trigger decision lives in `player.thunder_active_on_hit()` (Thunder God = always, else Plugged In's chance), shared by bullets, the sword and the aura. Pool is now **34** upgrades. **(2026-07-19) Bug fix — this item did nothing at all until now:** `thunder_burst` bailed out on `thunder_stacks <= 0`, which is exactly Plugged In's state, so the 10% roll won and then the chain silently aborted. The gate belongs to `thunder_active_on_hit()` alone; the duplicated check is gone.
- ✅ **New item — Thunder God** (Epic, `upgrade_38.png`): **chain lightning like Binding of Isaac's Jacob's Ladder**. When a **bullet, the Cursed Sword or the Extinguisher aura** hits an enemy, an electric arc jumps from **that enemy** to **every** enemy within `thunder_range` (**200 px**), dealing **25% of the player's damage** (`bullet_damage`). Enemies caught by the current flash a **blue electric tint** (`enemy.flash_electric()`). It doesn't chain further and never starts from the player — the animation originates at the struck enemy on impact. Art is `fx/electricity fx/electricity.png` (896×63) cut into 14 frames of 64×63; each arc is rotated toward the target (north→direction, like bullets) and **stretched vertically so its length equals the exact distance** between the two enemies (`scale.y = d / 63`). Pool is now **33** upgrades. **(2026-07-19)** The **aura fires one chain per pulse**, from a random enemy it hit — not one per enemy, which would be N×N arcs per pulse. The arc now **stays glued to the enemies like a rope** instead of hanging where it spawned: `electric_arc.gd` holds both end **nodes** and re-does position/rotation/length every frame. If an end dies mid-animation its position simply freezes.
- ✅ **New item — Stacked Armory** (Rare, `upgrade_37.png`): **"+1 projectile at a random enemy"**. A **guaranteed** extra projectile per pick (+1, +2, +3 …), but unlike Twin Comets' parallel shots next to the primary target, each bonus projectile is fired at a **different random enemy** — so you shoot in several directions at once. Guarantees the count even with few enemies (`_armory_targets` repeats/falls back to the primary). Bullet spawning was refactored into a shared `_spawn_one_bullet()`. Pool is now **32** upgrades.
- ✅ **New item — Broken Watch** (Uncommon, `upgrade_36.png`): **"50% chance to fire +1 projectile"**. Each volley has a **fixed 50% chance** to fire bonus projectiles; picking it again does **not** raise the chance, it raises **how many** bonus projectiles fire when it procs (+1, +2, +3 …). Like Stacked Armory, those bonus projectiles fire at **random other enemies** (not parallel to the main target). Bullet weapons only (pistol/mage). Implemented in `_fire_bullets` (`broken_watch_chance` / `broken_watch_stacks` on `player.gd`). Pool is now **31** upgrades.
- ✅ **Crit chance can now exceed 100% → multi-crit** (Brotato-style). The 100% cap is gone. The **integer part** of your crit chance is guaranteed crits, the **fractional part** is the chance of one more, and each crit tier multiplies damage by `crit_mult` (2×): **100% → 2×, 200% → 4×, 300% → 8×**; 150% = 50% ×2 / 50% ×4. Single source of truth is `player.roll_crit()` (returns `{tiers, mult}`), used by all three weapons (bullets, Extinguisher aura, Cursed Sword). Stacks with Megane's Katana, whose speed-scaled crit adds on top of Adrenaline's flat crit, so you can pass 100% at high speed without maxing Adrenaline.
- ✅ **Live stats panel in the level-up menu** (Binding of Isaac style). When the level-up screen opens, a **STATS** panel appears on the **right edge** of the screen (same `Menu.png` frame), listing 12 core stats: Damage, Attack Speed, Crit, Projectiles, Pierce, Weapon Size, Knockback, Instakill, Move Speed, Max HP, HP Regen, Damage Taken. Each is coloured by how it compares to the value you **started the run with** (captured after meta-progression in `player._ready()` as `_stats_base`): **grey** = unchanged, **green** = better, **red** = worse. Built in `levelup.gd` (`_build_stats_panel()` / `_refresh_stats()`); the numbers come from `player.stat_lines()`. Attack Speed (shown as a rate, e.g. `2.50/s`) and Damage Taken are "lower-is-better", so their comparison is inverted.

## Current state (2026-07-16)
- ✅ **New item — Alex's Protection** (Rare, `upgrade_28.png`): **+25% Max HP · +15% Move speed**, no downside. The percentages **compound** off the current values (max HP 100 → 125 → 156 → 195 over three picks), matching The Nightclub, the game's only other percentage upgrade. Raising max HP also heals you by the same amount.
- ✅ **New item — Stolen Halo** (Rare, `upgrade_29.png`): **+15 Damage · +5 Max HP**, and it **stacks** — every pick adds the same again. `bullet_damage` feeds every weapon (the sword deals `sword_base_damage + bullet_damage`). Its signature: a halo animation (`fx/halo fx`, 640×58 → 10 frames of 64×58) that floats **above the player for the rest of the run** — an `AnimatedSprite2D` child at `z_index` 1, looping, 76 px above his centre (his head tops out at 62). `show_halo()` adds it **once**, so picking the item three times gives three stacks but one halo. Size follows the Firewalker rule: `halo_size` in px, scale derived. Position knobs: `halo_side` (−2, negative = left) and `halo_height` (76) — these are *screen-space* and deliberately un-rotated, unlike the sword's offsets, because the player never rotates and the halo must stay over his head.
- ✅ **The slash tracks your facing while it swings (Megabonk-style).** It used to freeze its direction at spawn — start a slash facing west, turn east, and it stayed pointing west. `_update_slashes()` (from `_process`) now rewrites its position/rotation/scale every frame from the *current* facing. The **damage follows too**: the slash stays live for the animation, re-testing its disc each frame, so you can't swing at someone and have the hit land where you used to be looking. Each enemy is hit **once per slash** (tracked by instance ID, since enemies can die mid-swing) — the next slash can hit them again. Small deliberate buff: enemies that *walk into* the slash while it sweeps now get hit too.
- ✅ **Cursed Sword rebuilt on the Firewalker model** (`firetrail.gd`), with new art. Three things copied from it, and they fix the whole saga below:
  - **Size in pixels, not a multiplier** — `sword_size` (160) is the slash's width on screen; the sprite scale is derived (`sword_size / 64`), exactly like Firewalker's `size / 32.0`. Swap the art, the size stays.
  - **Hitbox derived from the art, not hand-tuned** — the envelope is measured at startup and the rectangle is cut from it, in art pixels, so it follows any size you set. Same spirit as Firewalker's `radius = size * 0.4`. **The art and the hitbox can no longer drift apart** — that was the bug that started all of this.
  - **Frames face west** — `rotation = dir.angle() - PI`, same convention as Firewalker's trail.
  - Art: `cursed sword fx.png` (768×55) → **12 frames of 64×55**. The frame width was found by detecting the drawing blocks per column and picking the only exact division of 768 where no drawing straddles a boundary. The new art is nearly symmetric (sweep centre −1.1° at `sword_lateral` 0, vs the old art's −13.8°).
  - Knobs: `sword_size`, `sword_reach`, `sword_lateral`, `sword_art_rotation`, `sword_anim_speed`, `sword_debug`. Set **`sword_debug = true`** to draw the hitbox live over the game — red rectangle = what hits, blue cross = where the art sits.
- ✅ **The slash looks identical in all 8 directions, just rotated**, and draws **under the player** (`z_index` −1 — slashing north used to paint the arc over his head). Every offset lives in **art space** and is rotated by your facing, through a single `_sword_offset(dir)`. Do **not** add un-rotated "screen" offsets: one was tried and it is exactly what breaks direction consistency (it drags the slash toward you at east but pushes it ahead at west).
- ✅ **Weapon-size upgrades no longer leave the hitbox behind.** The sprite was anchored at `(sword_reach, sword_lateral) × weapon_size_scale()` but the rectangle added those two *un-scaled* — identical at scale 1 (so every earlier test passed), yet with stacked Pufferfish / Rat's Burger the art reached 386 px while the hitbox stopped at 252. Both now come from one `_sword_offset_art()`. **Lesson: test sword geometry at several `weapon_size_px` / `weapon_size_mult` values, not just on a fresh player.**
- ✅ **Hitbox is a fixed rectangle cut from the animation's envelope.** In art space (x = forward, y = sideways), rotated by your facing: it starts at **0** (the player, so the gap between him and the crescent still deals damage), ends **forward at the animation's furthest pixel** (92 px), and **sideways at its furthest pixels** (−63…+67). It never changes during the sweep. The envelope is *measured at startup* from the frames' opaque pixels (`_masoara_arta_sabiei`), not hardcoded — swap the art and it recomputes; being in art pixels, it follows `sword_size`/`sword_reach`/`sword_lateral` automatically.
- ✅ **The hitbox is measured from the art, not guessed.** It originally hit at 135 px in a ±81° cone while the art only reached 107 px within ±62° — roughly **3× the visible area**, killing enemies past your shoulders. Shapes tried since, and why each was dropped: cone (the art wraps around the player at close reach, so fitting a forward cone yields ±180°) → disc (covers the hollow between the crescent's horns) → pixel-perfect 1:1 (exact, but left the gap between player and slash undamaged) → **rectangle**. Areas: disc 17,765 px², rectangle **11,960 px²**, 1:1 only 5,131 px².
- ⚠️ **Balance follow-up:** the honest hitbox covers far less than the broken one did, so the sword hits noticeably less than it originally felt. Not compensated yet — knobs are `sword_base_damage`, `sword_size` (the hitbox follows it automatically) and `sword_slow_start`.
- ✅ **New weapon — Cursed Sword** (4th selectable weapon in the menu). Auto-**slashes in the facing direction** every `fire_interval`. Base swing is intentionally **slow** (`sword_slow_start` = 1.9× interval when picked) so attack-speed upgrades feel impactful; scales with the player's damage / crit / knockback / instakill / weapon-size upgrades (modelled on the Extinguisher aura + bullet instakill).
- ✅ **Slash animation attached to the player.** The slash `AnimatedSprite2D` is a **child of the Player node** (not left behind in the world), so it moves with you — the vibe is that the sword is always in hand. Its position/scale divide by the player's `scale` (×2 in `main.tscn`) to stay in real pixels.
- ✅ **Upgrade tweaks** (`levelup.gd`): **Rabbit's Foot** now −5 damage · **+25%** attack speed (was +10%); **Grinder** rarity Rare → **Common**; **The Nightclub** rarity Epic → **Rare**; **Syringe → Last Resort** (new icon `upgrade_35.png`; was briefly "Knight's Power" / `upgrade_26.png`).

## Current state (2026-07-15)
- ✅ **New bullet art + orientation system.** The Pistol's default bullet now uses `bullets/bullet normal.png`. The source art points **north-east**, so its `Sprite2D` child carries a `-45°` rotation that makes the composite point "north" — the existing `set_direction()` (`+PI/2`) then aims it correctly in **all** directions. Same treatment for the new combined bullet.
- ✅ **Weird Concoction + Stroh synergy (combined bullet).** Taken **alone**, each keeps only its stat bonus and leaves the bullet **normal** (they no longer swap to `bullet2/bullet3`). Taken **together**, the Pistol fires a special **`bullet_combined.tscn`** (purple) — a hidden synergy like Godwalker. Tracked via `has_weird` / `has_stroh` on `player.gd`.
- ✅ **Instakill mechanic + 5 new upgrades.** Bullets can now roll an **instant kill** (`instakill_chance` on `player.gd`, passed to `bullet.gd`; shows a red number). New items: **The Nightclub** (Epic, +35% damage · −35% attack speed), **Rusty Hacksaw** (Uncommon, 1% instakill, +0.5%/stack), **Doctor's Hacksaw** (Legendary, 5% instakill, +2%/stack), **Rabbit's Foot** (Uncommon, −5 damage · +10% attack speed), **Mike's Hedgehog** (Epic, reflect 100% of contact damage back, once every 3s). Pool is now **23 upgrades**.
- ✅ **Extinguisher foam animation reworked.** A 14-frame spritesheet (`stingator/stingator.png`) is sliced to `frame_0..13.png` and wired into the aura's foam animation. A single knob **`foam_scale`** (`@export`) now sizes it, and the **hitbox equals the sprite always** — both derive from the same `radius`, so what you see is exactly what damages enemies.
- ✅ **Weapon-size upgrades now scale the trails.** Pufferfish & Rat's Burger multiply `weapon_size_scale()` into **Firewalker / Frostwalker / Godwalker** patch sizes (visual **and** damage radius), on top of Pistol/Mage/Extinguisher.
- ✅ **Upgrade rebalance & reskins:** **Papers → Rolling Papers** (now +10% attack speed instead of bullet speed → also speeds up the Extinguisher pulse); **Pufferfish** +30 → **+10** weapon size; **Syringe** +12 → **+7** damage; **Adrenaline** now also crits on the **Extinguisher** aura; **Parallel Bullets → Twin Comets** (+1 Projectile, `upgrade_19`); **Knockback Stick** new icon (`upgrade_22`). The Mage orb also got a **purple tint** (`modulate`) to match its impact explosion.
- ✅ **Fewer dead Extinguisher upgrades.** Only **Twin Comets** and **Drill** do nothing for it now; Rolling Papers (attack speed) and Adrenaline (aura crit) both work.

## Current state (2026-07-14)
- ✅ **Weapon size is now a real stat** (`weapon_size_px` + `weapon_size_mult` on `player.gd`). It grows **sprite *and* hitbox** for whichever weapon you picked: Pistol/Mage → the bullet (and the mage orb, which is a child of it); Extinguisher → the aura radius (which is both the drawn ring *and* the damage zone). Two new upgrades drive it: **Pufferfish** (Common, +30px) and **Rat's Burger** (Rare, +30%). Pool is now **18 upgrades**.
- ✅ **Mage Staff visuals fixed.** The magic orb was being drawn ~4px wide and was effectively invisible: it's a child of the bullet, whose root is `scale = 0.1` in `bullet.tscn`, so its own scale got multiplied by that. `_make_mage_orb` now divides by the parent's scale, so `mage_orb_size` (35) means *actual pixels on screen*. The impact explosion is tuned via `BOOM_VISUAL_SCALE` in `bullet.gd` (visual only — the AOE damage radius is untouched).
- ✅ **Extinguisher base damage raised to 15/pulse** (`aura_damage` 6→10; total is `aura_damage + 50% of bullet_damage`, so it still scales with damage upgrades).
- ✅ **Upgrade renames + new icons:** Cocaine→**Weird Concoction**, Weed→**Wine**, Hook→**Knockback Stick**, OCB Papers→**Papers**; new art for those plus Drill and Double Dose (`upgrade_12.webp`, `upgrade_13..18.png`).
- ⚠️ **Known issue (pre-existing):** the bullet's `CollisionShape2D` is a default `CapsuleShape2D` (radius 10) under a `0.1` root scale → a **1-pixel** hitbox against a 27px sprite. Bullets visually pass through enemies more than they should. Fix by enlarging the capsule in `bullet.tscn`.
- ⚠️ **Balance notes:** the **Pistol is strictly worse than the Mage Staff** (same damage, same fire rate, but the mage also gets a free AOE explosion) — it's a false choice. And the Extinguisher can still be offered **4 dead upgrades** (Papers, Parallel Bullets, Drill, Adrenaline), since it fires no bullets and its aura can never crit; the level-up pool doesn't filter by weapon.

## Current state (2026-07-08)
- ✅ **Frostwalker** (Epic) — a **frost trail** at your feet that **slows** enemies (blue tint on them) and does light damage. Per stack: +0.5s slow duration & +0.3s trail; damage stays. Mirror of Firewalker (`icetrail.gd`), art desaturated at load.
- ✅ **Godwalker** — having **Firewalker + Frostwalker** replaces both trails with a single combined one (`godtrail.gd`): fire damage **and** frost slow, its own animation.
- ⚠️ **World-gen spacing: every system must check against the OTHERS, not just itself.** This produced four separate "object grown through object" bugs (cactus in house, rock in cactus, statue in rock), all found by measuring, not by looking. The rules now: cacti always yield to houses\monuments (the chunk-key tie-break applies **only** between two objects that can both be skipped, else the never-skipped one wins); distances are measured between **hitbox centres** via `_footprint_center()`, not raw positions (the node is shifted by `sort_shift` and the collider by its own offset, differently per type); the neighbour scan radius is **computed** from the largest minimum distance (`_neighbor_radius()`) because a 512px chunk is smaller than the 691px cactus↔house gap; houses\monuments are placed per-**desert** with retries so they avoid each other; rocks and trees exclude themselves from the desert **per position**, not per chunk (a chunk-level test leaves a fringe where both are legal). Re-run the check after touching any of this.
- ✅ **Biome-aware world gen refined:** trees no longer spawn on the desert **gradient** (only pure forest); rocks stay out of desert-influenced ground entirely; new **desert structures** (`desert_structures.gd`, node `DesertStructures`): **cactus** scattered per-chunk (denser), **house** guaranteed 1–2 **per desert** (kept ≥20px inside, never on the gradient), **monument** ~once per 2 deserts (never on the gradient). Each structure has its own `scale` + hitbox via a `CONFIG` block.
- ✅ Smaller Jean's Bomb explosion animation (new frames + reduced on-screen scale).
- Trail spritesheets are sliced into frames **at runtime** (`AtlasTexture`), so dropping in a new PNG just needs a Godot import.

## Cumulative feature summary
*(Everything that works, regardless of when it landed — the dated sections above are the changelog. This block used to carry a "2026-07-19" heading too, which made it look like a second changelog entry for the same day.)*
- ✅ **Working:** player movement + follow camera · infinite world · procedural **trees + rocks** with collision (rectangle hitboxes, per-side tuning, min spacing) · **Y-sort depth** (props cover you when you walk behind them) · enemy chase AI · automatic spawner · player auto-fire + projectiles that **face the target** · enemy HP + death · player HP + contact damage.
- ✅ **Biomes:** grass + **desert**, generated as random square patches (side 6–20 chunks) via a shared deterministic map (`biome_map.gd` / `BiomeMap`), rendered with a soft-blend shader (`biome.gdshader`). Trees & rocks don't spawn in desert. Patch edges within 2 chunks of their macro-cell boundary **snap to it** (`EDGE_SNAP`, mirrored in the shader), so two neighbouring deserts either merge or stay ≥3 chunks apart — otherwise a 1-chunk grass corridor could cut a desert in half, with rocks growing in it.
- ✅ **Every run starts somewhere new:** the world is infinite and deterministic, so instead of reseeding generation (which would mean keeping `biome_map.gd` and `biome.gdshader` in lockstep) `spawner.gd` drops you at a **random point** in it each run. Same map math, brand-new surroundings.
- ✅ **HUD** (`hud.gd`, built in code): red health bar (top-left) + cyan XP bar (bottom) + "Nivel N" label.
- ✅ **XP / leveling:** enemies grant XP on death → XP bar fills → **level up pauses the game and shows 3 random of 15 upgrades** (`levelup.gd`), styled like *Megabonk* (ornate panel + **rarity borders** + exact border-matched colors + 2px text outline), with thematic drug/drink effects. All in-game **UI text is in English**.
- ✅ **Signature items:** **Jean's Bomb** (Legendary) — +20 damage & bullets **explode (AOE)** on impact (explosion animation + knocks enemies back). **Firewalker** (Epic) — leaves a **burning fire trail** at your feet while moving that damages enemies; each stack lasts longer & grows +5px.
- ✅ **Interactive statues + boss summon:** statues are spawned procedurally (**3% of chunks**, one each, `statues.gd`), each rolling one of three looks. Walk near one and a **big SUMMON button** appears pinned to the **left edge of the screen** (phone-friendly — see `interact_ui.gd`); pressing it plays sink-into-ground + **screen shake** → the **"Garda" boss** rises (8-directional walk) and **throws lightning-ball projectiles** at you from range. Each statue works once, and the boss scales with the run clock, so summoning late means a much tougher fight.
- ✅ **Enemy art:** the "homeless" enemy walks with **8-directional animations** (`enemy_frames.tres`, 6 frames each, sliced from GIFs into `homeless directii/frames/`).
- ✅ **Atmosphere:** the `Atmosphere` node (`atmosphere.gd`) owns the screen-space mood layers — a **vignette** and a subtle **glow/bloom**. The night tint + player light are written but commented out in `_ready()`.
- ✅ **Falling leaves under trees** (`leaffall.gd`): roughly **1 tree in 10** sheds leaves, rolled deterministically when its chunk loads (same tree, same answer, every time you walk back). The leaves drop inside a box covering the tree's **full width, from ~31% of its height down to just below the roots** — proportions measured off `harta/Tree Leaf Area.png`, where the owner drew the wanted area by hand on two different trees. They render **above** the tree and **fade to fully transparent** as they near the ground, so they melt into the grass instead of popping out. Each leaf is one of the 5 cut out of `harta/Leaf Overlay.png` (`AtlasTexture`), with its own speed, sway, spin and pause between drops. Odds live in `props.gd` (`leaf_chance`); look/feel constants are at the top of `leaffall.gd`.
- ✅ **Sound:** a global audio manager (`audio.gd`, autoload **`Audio`**) with a pooled `Audio.play("name")` API. The old code-generated SFX were **removed by the owner**; current assets are a looping **main menu theme** (`audio/main menu theme.ogg`) and a **UI click** (`audio/button.wav`, silence-trimmed) auto-wired to *every* menu button. Gameplay SFX calls (`shoot`/`hit`/`enemy_die`/`xp`/`levelup`/`hurt`) are still in the code and stay silent until you drop the files in `audio/` and add a line to the `SFX` dict.
- ✅ **Weapon upgrades & juice:** an effects workshop (`fx.gd`, autoload **`Fx`**) — muzzle flash, impact sparks (`CPUParticles2D`), floating damage numbers (crit = big yellow), all code-generated. New weapon stats: **crit** (double damage), **pierce** (bullets pass through enemies), **bullet size**, **knockback** (pushes enemies back). Four new level-up choices (Foraj/Adrenalină/Doză dublă/Croșeu) drive them; works on all 3 bullets. **Screen shake** on crit (trauma-based camera shake in `player.gd`).
- ✅ **10-minute run + Final Swarm (Megabonk-style):** waves are **gone**. A single **10:00 countdown** runs at the top of the screen while enemies stream in continuously, getting linearly tougher each minute (~6.5× HP by 10:00). When it hits zero the **Final Swarm** begins: the timer flips to counting **up** in red and scaling turns **exponential** — enemy HP doubles every 45s. It is not meant to be survivable forever; your score is how long you last. All the knobs live as constants at the top of `difficulty.gd`; the spawner (`spawner.gd`) converts `spawn_mult()` into a rate and spawns in batches once it outruns its own tick. The **boss** is no longer automatic — you summon it at the statue when you're ready, and it scales with the clock too. In-game background music is currently **off** (`Audio.MUSIC_GAME` is empty — set it to a file to bring it back).
- ✅ **Score:** the leaderboard records **time survived + level reached + kills**, and marks runs that made it past the 10 minutes as `SURVIVED`. Kill count is also live on the HUD (top-right).
- ⬜ **Next:** cyberpunk art polish · on-screen touch joystick · Android export · gameplay SFX (the calls are already wired, the files are missing) · in-game background music.

### Y-sorting rule (read before adding any world object)
The player's sprite is **centred** on its sort point, so it extends ~64px **below** it. An object whose art ends exactly at its own sort line can never fully cover the player — the legs stay sticking out underneath. Trees solve this with `sort_anchor` (art hangs 73.8px below the sort line) and the statue with `ACOPERIRE_JOS = 74`. **Any new object that should occlude the player needs its art to hang more than ~64px below its sort line.** If you move that art, move its `CollisionShape2D` by the same amount or the hitbox detaches from the sprite.

## How to run
Open the project in **Godot 4.7**, press **Run Project** (▶ / F5). Main scene is `main.tscn`. Move with the **arrow keys**; the player auto-fires. Touch controls come in a later milestone — for now, test on desktop.

## Working with the project owner ⭐ (READ THIS)
The owner (**Răzvan**) is a **complete beginner** — first game ever, first time in Godot, learning by building this project one step at a time. If you're an AI assistant helping:

- **Reply in Romanian.**
- **Teach, don't just do.** Introduce each new concept simply (analogies help) before/while using it. Move **one small, testable step at a time** — every step should end with "press Run and see X".
- **Avoid copy-paste pain.** They've hit repeated *"mixed tabs and spaces"* errors and once pasted code into the wrong file. Prefer **writing `.gd` files directly** (with correct tab indentation) and letting them do only the editor/node parts. Always remind them: after any paste, **Edit → Convert Indent to Tabs**.
- **If code ends up misbehaving, check filenames first** — each script belongs to its node (`player.gd` → Player, `bullet.gd` → Bullet, etc.).
- **Be concrete about the Godot UI:** exact panels, buttons, and node names. They don't know the interface from memory yet.
- **Node names are load-bearing:** scripts reference children by exact name (e.g. `hud.gd` needs a child `ProgressBar` named `HealthBar`).

**Roadmap (survivors-like):** 1) movable player ✅ · 2) chasing enemy ✅ · 3) spawner ✅ · 4) auto-attack + enemy death ✅ · 5) HP + health bar ✅ · 6) XP + level up + item choice ✅ · 7) difficulty scaling ⬜ · 8) cyberpunk art/sound ⬜ · 9) Android export + touch controls ⬜.
